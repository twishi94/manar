export function Industries({ activeTab }: { activeTab: string }) {

  return (
    <section id="industries" className="py-12 lg:py-16">
      {/* Industries content can be added here if needed */}
    </section>
  );
}